---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: centos-6.6
  - name: ubuntu-14.04
    driver_config:
      network:
        - ["forwarded_port", {guest: 8080, host: 8082}]

        
suites:
  - name: default
    run_list:
      - recipe[chef-cd-workshop::default]
    attributes:
